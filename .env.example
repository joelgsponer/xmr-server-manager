# Cloudflare API Configuration
# 
# To get these values:
# 1. API Token: https://dash.cloudflare.com/profile/api-tokens
#    - Create a token with "Zone:DNS:Edit" permissions
# 2. Zone ID: Found on your domain's overview page in Cloudflare dashboard
# 3. DNS Name: The subdomain you want to manage

# Cloudflare API Token (required)
CF_API_TOKEN=your_cloudflare_api_token_here

# Cloudflare Zone ID (required)
CF_ZONE_ID=your_zone_id_here

# DNS Name to manage (required)
DNS_NAME=xmr.yourdomain.com

# Log level (optional, defaults to INFO)
# Options: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO
